{
  "name": "gulp-replace-file",
  "version": "1.1.2",
  "description": "gulp plugin replace file",
  "main": "index.js",
  "scripts": {
    "test": "cd test && gulp replace",
    "minor": "release-it --ci minor",
    "patch": "release-it --ci patch",
    "release": "release-it --ci"
  },
  "author": "helondeng",
  "license": "MIT",
  "dependencies": {
    "css2string": "^1.0.3",
    "decomment": "^0.8.6",
    "parse-tpl": "^1.0.3",
    "through2": "^2.0.1"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "@release-it/conventional-changelog": "^7.0.0",
    "release-it": "^16.1.2"
  }
}
